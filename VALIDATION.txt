Validation performed in the artifact environment
=================================================

1. Static build-contract regression:
   python -m pytest -q tests/zynnova/test_zynmorph_tetgen_build_contract.py
   Result: 4 passed

2. CMake install-rule simulation with:
   - required TetGen source/license files present
   - optional README absent
   - optional VENDOR_MANIFEST absent
   Result: cmake --install succeeded and staged only the required files.

3. The user's Windows log already demonstrates that the real TetGen 1.6.0
   source compiles and links successfully through target 17/17; the observed
   failure occurs after linking, during CMake wheel staging, at the previously
   unconditional README.md install entry.

The artifact environment is not the user's Windows/MinGW environment, so the
final editable-wheel rebuild must be rerun locally after applying this patch.
